name: academicollab

services:
  # Hardened Database Storage Component
  db:
    build:
      context: .
      dockerfile: docker/postgres/Dockerfile
    image: academicollab-postgres:16-patched
    container_name: academicollab_db
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - academicollab_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Redis Cache Broker Service Dependency
  redis:
    image: redis:7-alpine
    container_name: academicollab_redis
    ports:
      - "6379:6379"
    networks:
      - academicollab_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # Django Core Application Node
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: academicollab_backend
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DEBUG=${DEBUG}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=db
      - DB_PORT=5432
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}
      - XDG_CACHE_HOME=/tmp/.cache
    networks:
      - academicollab_net
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  # Dedicated Celery Background Compute Engine
  celery_worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: academicollab_celery_worker
    command: celery -A config worker --loglevel=info
    volumes:
      - ./backend:/app
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DEBUG=${DEBUG}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=db
      - DB_PORT=5432
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    networks:
      - academicollab_net
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  # React Frontend Dashboard Node
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: academicollab_frontend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    networks:
      - academicollab_net

volumes:
  postgres_data:

networks:
  academicollab_net:
    driver: bridge